# 작업 폴더 동기화 (OPCG)

- 이 리포의 `OPCG/` 폴더는 사용자 PC의 `C:\Users\정민혁\Documents\OPCG` 작업 폴더와 동기화되는 사본이다. 작업 파일을 읽거나 고칠 때는 이 폴더를 기준으로 한다.
- **로컬(Windows) 세션이면**: 작업을 시작할 때 먼저 리포 루트의 `sync-opcg.bat`(또는 `scripts/sync-opcg.ps1`)을 실행해 동기화한 뒤 작업하고, 작업을 마치면 한 번 더 실행해 변경분을 push한다.
- **클라우드/웹 세션이면**: 사용자 PC에 접근할 수 없으므로 `OPCG/`의 파일을 직접 수정하고 push한다. 사용자가 PC에서 동기화 스크립트를 실행하면 작업 폴더로 반영된다.
- 동기화 규칙: 더 최신 파일이 이긴다. 파일 삭제는 전파되지 않는다. 95MB 초과 파일은 동기화에서 제외된다.
