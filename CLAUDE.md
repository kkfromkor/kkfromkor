# 작업 폴더 동기화 (OPCG)

- 이 리포의 `OPCG/` 폴더는 사용자 PC의 `C:\Users\정민혁\Documents\OPCG` 작업 폴더와 동기화되는 사본이다.
- **작업 기준 경로 (사용자, 2026-08-06)**: 로컬(Windows) 세션에서는 작업 폴더 **원본** `C:\Users\정민혁\Documents\OPCG`의 파일을 직접 읽고 고친다. 리포의 `OPCG/` 사본을 직접 고치는 것은 클라우드/웹 세션뿐이다. (두 경로를 헷갈리지 말 것 — 로컬 원본이 기준이다.)
- **로컬(Windows) 세션이면**: 작업을 시작할 때 먼저 리포 루트의 `sync-opcg.bat`(또는 `scripts/sync-opcg.ps1`)을 실행해 최신을 받고, 작업은 위 작업 폴더 원본에서 한 뒤, 마치면 한 번 더 실행해 변경분을 push한다.
- **클라우드/웹 세션이면**: 사용자 PC에 접근할 수 없으므로 `OPCG/`의 파일을 직접 수정하고 push한다. 사용자가 PC에서 동기화 스크립트를 실행하면 작업 폴더로 반영된다.
- 동기화 규칙: 더 최신 파일이 이긴다. 파일 삭제는 전파되지 않는다. 95MB 초과 파일은 동기화에서 제외된다.
- **선택 동기화**: 작업 폴더 전체는 약 22GB라 전체 동기화를 하지 않는다. `scripts/sync-include.txt`(OPCG 기준 상대 경로, 한 줄 하나)에 적힌 하위 폴더만 동기화된다. 동기화 대상 합계는 1.5GB를 넘기지 않는다. `pull:` 접두사 항목은 받기 전용(리포→작업 폴더만)으로, PC 쪽 대용량 폴더(예: 카드 이미지 pics)에 새 파일만 내려줄 때 쓴다.
- `OPCG/_inventory.txt`는 PC 작업 폴더의 **전체 파일 목록**(상대경로 + 크기, 매 동기화마다 갱신)이다. 필요한 파일이 리포에 없으면 이 목록에서 찾아 해당 폴더를 `sync-include.txt`에 추가·push하고, 사용자에게 동기화 스크립트 재실행을 요청한다.
- **전체 유저 배포**: 유저 클라이언트들은 GitHub의 `ClaudeFable5/OPTCG` 리포(공개)에서 카드 데이터를 자동 업데이트 받는다. 이 세션(kkfromkor 소유 리포 세션)에서는 계정 간 제약으로 그 리포에 push할 수 없으므로, 사용자가 `ClaudeFable5/OPTCG`로 연 별도 Claude 세션이 리포 루트의 `DEPLOY_TO_OPTCG.md` 지시서에 따라 배포한다. 카드 작업을 마치면 사용자에게 그 세션에서 배포를 트리거하라고 안내한다.
- **게임 반영(로컬 배포)**: 카드 데이터(cdb/lua/pics)를 이 컴퓨터 게임·서버에 꽂을 때는 `sync-and-deploy.bat`(= `scripts/deploy-opcg.ps1`)을 실행하게 한다. 동기화 후 기존 `tools/deploy_all.ps1`과 같은 방식(전체 미러 + 유령 파일 정리)으로 캐논(F: CODEX)과 배포처들(E:\github\OPTCG, 서버 드롭 폴더, 로컬 클라이언트 등)에 복사하고, OPTCG repo 커밋/push와 서버 적용하기.bat 실행까지 자동으로 한다.
- **상시 승인 (사용자, 2026-08-04)**: **카드 추가·카드 쪽 버그 수정 등 카드 작업**(cdb/lua/pics/opcg_data, expansions 하위)은 사용자 확인 없이 바로 리포에 커밋·push하고 배포(sync-and-deploy 흐름: 배포처 미러 + OPTCG repo push + 서버 적용)까지 진행한다. **UI 변경을 포함해 코어를 건드리는 작업**(gframe 클라이언트 코드, ocgcore, 엔진 C++, exe 빌드, update.json/릴리스)은 반드시 사용자 승인을 거친 뒤 진행한다. 카드 작업인지 코어 작업인지 애매하면 코어로 취급하고 물어본다.
